# Test writing an instantaneous time series to ReclamationHDB
#   * development database
#   * real table
#   * SDI 100380
#   * instantaneous data (treated as irregular)
#   * Write twice with different numbers to make sure old results are not just sitting
#     in the database.  Write non-missing values because HDB convention is to
#     NOT write missing values (not sure how re-write is handled if missing involved).
#   * For two writes, use TSID read once, and ReadReclamationHDB() once to exercise read.
StartLog(LogFile="Results/Test_WriteReclamationHDB_Dev_Instant_AAADELETE_Real.TSTool.log")
RemoveFile(InputFile="Results/Test_ReclamationHDB_Dev_Instant_AAADELETE_Real_out.dv",IfNotFound=Ignore)
# Make the definition of this time series match what is used in HDB so that the comparison
# of results all matches
#
# First pass...
NewPatternTimeSeries(Alias="AAA_DELETE",NewTSID="AAA_DELETE.HDB.flow.Irregular",IrregularInterval=15Minute,Description="AAA_DELETE",SetStart="2010-01-01 00:00",SetEnd="2010-01-03 12:30",Units="cfs",PatternValues="5,10,12,13,75")
# First write the time series to the database...
WriteReclamationHDB(DataStore="ReclamationHDB-ECO-Dev",SiteCommonName="AAA_DELETE",DataTypeCommonName="inst flow",SiteDataTypeID=100380,ValidationFlag="A")
# Now read the same time series from the database using the period that was written...
SetInputPeriod(InputStart="2010-01-01 00:00",InputEnd="2010-01-03 12:00")
ReadTimeSeries(TSID="diversion:100380.HDB.inst flow.Irregular~ReclamationHDB-ECO-Dev",Alias="FirstReadUsingTSID",IfNotFound=Warn)
#
# Second pass (previous values as hundreds)...
NewPatternTimeSeries(Alias="AAA_DELETE",NewTSID="AAA_DELETE.HDB.flow.Irregular",IrregularInterval=15Minute,Description="AAA_DELETE",SetStart="2010-01-01 00:00",SetEnd="2010-01-03 12:30",Units="cfs",PatternValues="500,100,120,130,750")
# First write the time series to the database...
WriteReclamationHDB(DataStore="ReclamationHDB-ECO-Dev",TSList=LastMatchingTSID,TSID="AAA_DELETE",SiteCommonName="AAA_DELETE",DataTypeCommonName="inst flow",SiteDataTypeID=100380,ValidationFlag="A")
# Now read the same time series from the database using the period that was written...
ReadReclamationHDB(DataStore="ReclamationHDB-ECO-Dev",Interval="Irregular",SiteCommonName="AAA_DELETE",DataTypeCommonName="inst flow",SiteDataTypeID=100380,InputStart="2010-01-01 00:00",InputEnd="2010-01-03 12:00",Alias="SecondRead")
#
# Uncomment the following command to regenerate the expected results file.
# WriteDateValue(OutputFile="ExpectedResults/Test_WriteReclamationHDB_Dev_Instant_AAADELETE_Real_out.dv",OutputStart="2010-01-01 00:00",OutputEnd="2010-01-03 12:30",TSList=LastMatchingTSID,TSID="AAA_DELETE.HDB.flow.Irregular")
# Generate new output for what was read from the database...
WriteDateValue(OutputFile="Results/Test_WriteReclamationHDB_Dev_Instant_AAADELETE_Real_out.dv",OutputStart="2010-01-01 00:00",OutputEnd="2010-01-03 12:30",TSList=LastMatchingTSID,TSID="AAA_DELETE.HDB.flow.Irregular")
# Do regression comparison...
CompareFiles(InputFile1="ExpectedResults\Test_WriteReclamationHDB_Dev_Instant_AAADELETE_Real_out.dv",InputFile2="Results/Test_WriteReclamationHDB_Dev_Instant_AAADELETE_Real_out.dv",IfDifferent=Warn)
