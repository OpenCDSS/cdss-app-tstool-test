#@enabled False
# Disable because should only be testing against development database
#
# Test writing a day time series to ReclamationHDB database:
#   * development database
#   * model table
#   * site common name AAA_DELETE
#   * daily data
#   * Write twice with different numbers to make sure old results are not just sitting
#     in the database.  Write non-missing values because HDB convention is to
#     NOT write missing values (not sure how re-write is handled if missing involved).
#   * For two writes, use TSID read once, and ReadReclamationHDB() once to exercise read.
StartLog(LogFile="Results/Test_WriteReclamationHDB_Dev_Day_AAADELETE_Model_Name.TSTool.log")
RemoveFile(InputFile="Results/Test_ReclamationHDB_Dev_Day_AAADELETE_Model_Name_out.dv",IfNotFound=Ignore)
# Make the definition of this time series match what is used in HDB so that the comparison
# of results all matches
#
# Start first pass...
NewPatternTimeSeries(Alias="AAA_DELETE",NewTSID="Model:AAA_DELETE.HDB.flow.Day",Description="Model:AAA_DELETE",SetStart="2010-01-01",SetEnd="2011-03-03",Units="cfs",PatternValues="5,10,12,13,75")
# First write the time series to the database...
WriteReclamationHDB(DataStore="ReclamationHDB-Dev",SiteCommonName="AAA_DELETE",DataTypeCommonName="flow",ModelName="DSS DATA LOADER",ValidationFlag="P")
# Now read the same time series from the database using the period that was written...
SetInputPeriod(InputStart="2010-01-01",InputEnd="2011-03-03")
Model:AAA_DELETE.HDB.flow.Day~ReclamationHDB-Dev
#
# Start second pass (previous values x 10)...
NewPatternTimeSeries(Alias="AAA_DELETE",NewTSID="Model:AAA_DELETE.HDB.flow.Day",Description="Model:AAA_DELETE",SetStart="2010-01-01",SetEnd="2011-03-03",Units="cfs",PatternValues="500,100,120,130,750")
# First write the time series to the database...
WriteReclamationHDB(DataStore="ReclamationHDB-Dev",TSList=LastMatchingTSID,TSID="AAA_DELETE",SiteCommonName="AAA_DELETE",DataTypeCommonName="flow",ValidationFlag="A")
# Now read the same time series from the database using the period that was written...
ReadReclamationHDB(DataStore="ReclamationHDB-Dev",Interval="Day",SiteCommonName="AAA_DELETE",DataTypeCommonName="flow",InputStart="2010-01-01",InputEnd="2011-03-03")
#
# Uncomment the following command to regenerate the expected results file
# (will have 4 time series:  originals, and copies read from database).
# WriteDateValue(OutputFile="ExpectedResults/Test_WriteReclamationHDB_Dev_Day_AAADELETE_Model_Name_out.dv",OutputStart="2010-01-01",OutputEnd="2011-03-03",TSList=LastMatchingTSID,TSID="Model:AAA_DELETE.HDB.flow.Day")
# Generate new output for what was read from the database...
WriteDateValue(OutputFile="Results/Test_WriteReclamationHDB_Dev_Day_AAADELETE_Model_Name_out.dv",OutputStart="2010-01-01",OutputEnd="2011-03-03",TSList=LastMatchingTSID,TSID="Model:AAA_DELETE.HDB.flow.Day")
# Do regression comparison...
CompareFiles(InputFile1="ExpectedResults\Test_WriteReclamationHDB_Dev_Day_AAADELETE_Model_Name_out.dv",InputFile2="Results/Test_WriteReclamationHDB_Dev_Day_AAADELETE_Model_Name_out.dv",IfDifferent=Warn)
