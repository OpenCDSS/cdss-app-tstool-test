# Test starting a new log file
# -Specify the name using ${TempDir} processor property (note contains trailing separator)
# -Just make sure no error results
StartLog(LogFile="${TempDir}Test_StartLog.TSTool.log")
