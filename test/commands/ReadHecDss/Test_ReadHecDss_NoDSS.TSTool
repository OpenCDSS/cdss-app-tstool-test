# Test reading non-existent DSS file:
# - should generate a warning in discovery mode
# - should generate a failure when run
#@os Windows
#
#@expectedStatus Failure
#
StartLog(LogFile="Results/Test_ReadHecDss_NoDSS.TSTool.log")
ReadHecDss(InputFile="Data\BadDSSName.dss")
