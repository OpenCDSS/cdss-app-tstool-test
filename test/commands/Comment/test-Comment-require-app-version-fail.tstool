StartLog(LogFile="Results/test-Comment-require-app-version-fail.tstool.log")
# Test that TSTool application is valid
# - use a future version
# - this should fail the requirements check and result in a failure for automated testing
#@expectedStatus Failure
#@require application TSTool version >= 999.999.999
