StartLog(LogFile="Results/test-Comment-require-hydrobase-version-fail.tstool.log")
# Test that HydroBase application is valid:
# - use a future version
# - this should fail requirements checks and result in a failed automated test
#@expectedStatus Failure
#@require datastore HydroBase version >= 99998877
