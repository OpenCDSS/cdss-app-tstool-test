StartLog(LogFile="Results/test-Comment-require-app-version-pass.tstool.log")
# Test that TSTool application is valid:
# - use an old version
# - this should pass since the requirements check is for an early version of TSTool
#@require application TSTool version >= 1.0.0
