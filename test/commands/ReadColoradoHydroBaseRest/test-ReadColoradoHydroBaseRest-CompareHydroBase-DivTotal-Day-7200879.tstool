StartLog(LogFile="Results/Test_ReadColoradoHydroBaseRest_CompareHydroBase_DivTotal_Day_7200879.TSTool.log")
# Test reading daily diversion record from REST web services and compare to HydroBase:
# - since daily time series, fill carry forward should occur (see "c" flags)
# - there should filled values at beginning and end of the irrigation years
# - do not fill with diversion comments
# - read all data but only compare the time series from start to recent (but not current year),
#   which allows the test to be run on relatively current HydroBase
ReadColoradoHydroBaseRest(DataStore="HydroBaseWeb",DataType="DivTotal",Interval="Day",Where1="StructureWDID;Matches;7200879",Alias="%L-WebService")
ReadHydroBase(DataStore="HydroBase",DataType="DivTotal",Interval="Day",Where1="Structure WDID;Matches;7200879",Alias="%L-Database")
#
CompareTimeSeries(TSID1="7200879-WebService",TSID2="7200879-Database",Tolerance=".01",AnalysisEnd="2024-10-31",CreateDiffTS="True",IfDifferent="Warn")
